# usage : uv run torchrun --nproc-per-node=8 examples/llm_finetune/finetune.py -c /home/firm/training/conf/automod/qwen3_32b_squad.yaml

step_scheduler:
  global_batch_size: 32
  local_batch_size: 1
  ckpt_every_steps: 50
  val_every_steps: 50  # will run every x number of gradient steps
  num_epochs: 5
  # max_steps: 100 

dist_env:
  backend: nccl
  timeout_minutes: 1

rng:
  _target_: nemo_automodel.components.training.rng.StatefulRNG
  seed: 1111
  ranked: true

model:
  _target_: nemo_automodel.NeMoAutoModelForCausalLM.from_pretrained
  pretrained_model_name_or_path: Qwen/Qwen3-32B
  torch_dtype: bf16
  attn_implementation: "sdpa" # use SDPA instead of flash_attention_2 (default)

checkpoint:
  enabled: true
  checkpoint_dir: /home/firm/training/ckpts/automod-qwen32b-csld
  model_save_format: safetensors # torch_save(use for training resumption, PEFT models) or safetensors(inference)
  save_consolidated: true # true if want to use model quick (inference ,deploy , etc.)

distributed:
  _target_: nemo_automodel.components.distributed.fsdp2.FSDP2Manager
  dp_size: none
  tp_size: 1
  cp_size: 1
  sequence_parallel: true

peft:  
  _target_: nemo_automodel.components._peft.lora.PeftConfig
  match_all_linear: True    # Apply LoRA to ALL linear layers  
  dim: 8                    # Low-rank dimension (small = more memory savings)  
  alpha: 32                 # Scaling factor  
  use_triton: True          # Use optimized Triton kernels


loss_fn:
  _target_: nemo_automodel.components.loss.masked_ce.MaskedCrossEntropy

dataset:
  _target_: nemo_automodel.components.datasets.llm.squad.make_squad_dataset
  dataset_name: /home/firm/training/data/fintune-data/automodel/squad_data/split-data
  split: train

packed_sequence:
  # Set packed_sequence_size > 0 to run with packed sequences
  packed_sequence_size: 0

dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: nemo_automodel.components.datasets.utils.default_collater
  shuffle: true


validation_dataset:
  _target_: nemo_automodel.components.datasets.llm.squad.make_squad_dataset
  dataset_name: /home/firm/training/data/fintune-data/automodel/squad_data/split-data
  split: validation

validation_dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: nemo_automodel.components.datasets.utils.default_collater

test_dataset:  
  _target_: nemo_automodel.components.datasets.llm.squad.make_squad_dataset  
  dataset_name: /home/firm/training/data/fintune-data/automodel/squad_data/split-data
  split: test

optimizer:
  _target_: torch.optim.Adam
  betas: [0.9, 0.999]
  eps: 1e-8
  lr: 5.0e-6
  weight_decay: 0
  # min_lr: 1.0e-5

wandb:
  project: qwen3_32b_squad_automod
